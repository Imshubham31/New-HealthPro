import { DatefunctionsPipe } from './datefunctions.pipe';

xdescribe('DatefunctionsPipe', () => {
    it('create an instance', () => {
        const pipe = new DatefunctionsPipe();
        expect(pipe).toBeTruthy();
    });
});
